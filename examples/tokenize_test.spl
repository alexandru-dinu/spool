   1 2 + pop
# checking the correctness of the tokenizer "hax hax"
func noop do end # no args, no body
call noop
"a" "_" + "b" + peek pop
"a b"       peek          pop # tabs should work, too
1 2 + pop "xy zt" "  " + "pq" + peek pop
"abc d ef"#comment w/o space

4#another

!! peek pop


34 35 + 10 * peek pop


func mymy x y z # no arity, just args
do
    @x @y + @z +
end

1 2 3 call mymy
dump#end
